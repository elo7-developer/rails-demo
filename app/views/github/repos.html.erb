<h1>Repos for <%= @user %></h1>

<% if @repos.nil? %>
  <p>Could not find repos</p>
<% elsif !@repos.blank? %>
  <ul>
    <% for repo in @repos %>
      <li><%= repo['name'] %></li>
    <% end %>
  </ul>
<% else %>
  <p>This user has no repos</p>
<% end %>
